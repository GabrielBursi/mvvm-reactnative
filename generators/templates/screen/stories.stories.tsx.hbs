import type { Meta, StoryObj } from '@storybook/react';

import {{pascalCase name}}Screen  from "./{{pascalCase name}}";

const meta: Meta<typeof {{pascalCase name}}Screen> ={
    title: 'Screens/{{pascalCase name}}Screen',
    component: {{pascalCase name}}Screen,
		args: {},
		argTypes: {},
}
export default meta

type Story = StoryObj<typeof {{pascalCase name}}Screen>;

export const Basic: Story = {
    args: {

    }
}
